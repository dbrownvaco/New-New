/**
 * By default, the app does not render a 'Get Guide' button. If 'getGuide' is
 * defined, it will be called when the user hits the 'Get Guide' button.
 * It should specify detailed guidance for users of the app. Specifically, it
 * should return an object with a field called 'markdown', which should be
 * a string in markdown syntax. See https://www.markdownguide.org/ and the
 * MARKDOWN section in the README. This markdown will be rendered in the app.
 * Comment to test
 */
// function getGuide() {
//   return {markdown: `# Guide text, defined in Markdown`}
// }